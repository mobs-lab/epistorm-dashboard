//TODO: The Settings Pane (invoked by clicking on "settings" button on top-right corner in dashboard)
//  Should be collapsed by default and slide out right->left to display a list of settings, which should persist state even when user click elsewhere or go to different routes (add state later)